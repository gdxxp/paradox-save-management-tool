export interface FormItem {
    name: string;
    location: string;
    updateTime: string;
    isBackups: boolean;
    isChecked: boolean;
}

export interface ListenParams {
    location: string;
    updateTime: string;
}